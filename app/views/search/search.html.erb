<div class="col-md-8 offset-md-1">
  <h2>Book search for <%= @content %></h2>
    <% if @model == 'user' %>
      <%= render 'users/search', users: @datas %>
    <% else @model == 'book' %>
      <%= render 'books/search', books: @datas %>
    <% end %>
</div>